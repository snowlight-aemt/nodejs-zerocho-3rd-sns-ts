const Sequelize = require('sequelize');

class User extends Sequelize.Model {
    static initiate(sequelize) {
        // 모델(테이블) 정보들
        // 모델 관계
    }

    static asociate(db) {
        
    }
}

module.exports = User;